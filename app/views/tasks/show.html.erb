<div class="d-grid gap-3">
  <center><div class="text-bg-warning p-2 border "><%= render @task %></div></center>
  <div><% User.where(id: @task.user_id).each do |user|%></div>
  <div class="text-bg-light p-1 border"><%= render "users/user", :user => user %></div>
</div>
<%end%>
 <center> <br><br><%= link_to t('action.edit_task'), edit_task_path(@task), class:"btn btn-warning" %>
<%= link_to t('action.back_task'), tasks_path, class:"btn btn-warning" %>
  <br><br><%= button_to t('action.destroy_task'), @task, method: :delete , class:"btn btn-warning "%></center>


